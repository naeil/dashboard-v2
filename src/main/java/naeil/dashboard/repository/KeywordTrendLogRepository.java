package naeil.dashboard.repository;

import naeil.dashboard.entity.KeywordTrendLog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface KeywordTrendLogRepository extends JpaRepository<KeywordTrendLog, Long> {
}
